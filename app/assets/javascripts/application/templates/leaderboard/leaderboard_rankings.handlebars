{{#each user in model}}
  <li {{ bindAttr data-user-id="user.id" }}>
    <span class="score">{{ user.score }}</span>
    <div class="photo"></div>
    <span class="name">{{ user.name }}</span>
    <div class="clearboth"></div>
  </li>
{{/each}}